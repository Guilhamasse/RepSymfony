<form method="post" action="{{ path('app_chanson_delete', {'id': chanson.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette chanson ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chanson.id) }}">
    <button class="button-delete">Supprimer la chanson</button>
</form>
