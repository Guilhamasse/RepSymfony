{% extends 'base.html.twig' %}

{% block title %}Hello DiscothequeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

</style>

<a href="{{ path('app_chanson_new') }}" class="button-create">Créer une chanson</a>
<div class="articles">
		{% for chanson in chansons %}
			{# <a href="{{ path('app_single_article' , {'slug' : article.slug}) }}" class="article-item"> #}
			<a href="{{ path('app_single_chanson' , {'id' : chanson.id}) }}" class="article-item">
			{# <div class="article-item"> #}
				<div class="article-image">
					<img src={{ chanson.photoCouverture }} width="150" height="150" alt="Image de la chanson : {{ chanson.titre }}">
				</div>
				<div class="article-details flex">
					<div class="article-author-details">
						<div class="article-content-details">
							<h2 class="article-title">
								Titre : {{chanson.titre}}</h2>
							<div class="article-createdAt">
								<p>Genre : {{ chanson.genre }}</p>
                                <p>Langue : {{ chanson.langue }}</p>
								{# <p>Date de sortie : {{ chanson.dateSortie }}</p> #}
							</div>
						</div>

					</div>
				</div>
			{# </div> #}
			</a>
			{# </a> #}


		{% endfor %}

	</div>
{% endblock %}
